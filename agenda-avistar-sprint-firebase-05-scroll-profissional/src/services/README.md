# Serviços

Esta pasta está preparada para a próxima etapa da refatoração. Na Sprint seguinte, as operações de Firebase serão extraídas de `App.jsx` para arquivos como:

- `demandasService.js`
- `usuariosService.js`
- `driveService.js`

Nesta primeira refatoração, a lógica foi mantida no `App.jsx` para reduzir o risco de regressão funcional.
