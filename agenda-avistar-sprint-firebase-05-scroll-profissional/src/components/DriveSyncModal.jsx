import React, { useState } from 'react';
import { X } from 'lucide-react';

export default function DriveSyncModal({config,onClose,onSave}){const [local,setLocal]=useState(config);return <div className="modal-backdrop"><div className="modal sync-modal"><div className="modal-title"><div><h2>Integração com Google Drive</h2><p>Cole a URL do aplicativo da Web criado no Google Apps Script.</p></div><button onClick={onClose}><X/></button></div><div className="sync-help"><strong>Planilha de destino:</strong><span>Planilha de Demandas - Avistar Operacional</span><small>A sincronização usa o ID único da demanda para criar ou atualizar a mesma linha.</small></div><div className="field full"><label>URL do Google Apps Script</label><input placeholder="https://script.google.com/macros/s/.../exec" value={local.webAppUrl||''} onChange={e=>setLocal({...local,webAppUrl:e.target.value.trim()})}/></div><label className="checkbox-line"><input type="checkbox" checked={local.autoSync!==false} onChange={e=>setLocal({...local,autoSync:e.target.checked})}/> Sincronizar automaticamente ao salvar, concluir ou excluir uma demanda</label><div className="modal-actions"><button className="secondary" onClick={onClose}>Cancelar</button><button className="primary" onClick={()=>onSave(local)}>Salvar configuração</button></div></div></div>}
