import React from 'react';
import { deadlineInfo } from '../utils/taskUtils';

export default function CalendarView({month,setMonth,tasks,onEdit}){const [y,m]=month.split('-').map(Number),first=new Date(y,m-1,1),last=new Date(y,m,0),start=first.getDay(),cells=[];for(let i=0;i<start;i++)cells.push(null);for(let d=1;d<=last.getDate();d++)cells.push(d);return <section className="calendar-panel"><div className="calendar-header"><h2>{first.toLocaleDateString('pt-BR',{month:'long',year:'numeric'})}</h2><input type="month" value={month} onChange={e=>setMonth(e.target.value)}/></div><div className="weekdays">{['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'].map(d=><span key={d}>{d}</span>)}</div><div className="calendar-grid">{cells.map((d,i)=>{const date=d?`${month}-${String(d).padStart(2,'0')}`:'',items=tasks.filter(t=>t.dueDate===date);return <div className={`day ${!d?'muted':''}`} key={i}>{d&&<strong>{d}</strong>}{items.slice(0,4).map(t=><button className={`cal-task ${deadlineInfo(t).kind}`} key={t.id} onClick={()=>onEdit(t)}>{t.title}</button>)}{items.length>4&&<small>+{items.length-4} demandas</small>}</div>})}</div></section>}
